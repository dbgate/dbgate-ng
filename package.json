{
  "private": true,
  "name": "dbgate-all",
  "pnpm": {
    "onlyBuiltDependencies": [
      "electron",
      "esbuild"
    ]
  },
  "scripts": {
    "dev:web": "pnpm -r --filter dbgate-web dev",
    "dev:app": "pnpm -r --filter dbgate dev",
    "ts:watch": "tsc -b -w"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}